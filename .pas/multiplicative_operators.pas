program multiplicative_operators;
var
a,b,result : integer;
ttrue, tfalse: boolean;

begin
   a := 20;
   b := 50;
   ttrue := true;
   tfalse := false;

   result := a * b;
   
   result := a / b;

   if (ttrue and tfalse) then
      tresult := true
end.